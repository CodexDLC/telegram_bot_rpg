# 🧭 Exploration & Encounters System (Design Document)

> **Status:** Draft
> **Focus:** Архитектура перехода между исследованием мира и боевыми столкновениями.

## 1. Проблема и Решение

### Проблема
Текущая реализация перемещения имеет риск нарушения **Single Responsibility Principle**:
1.  Если оставить логику проверок в хэндлерах (`apps/bot/handlers/...`), код станет перегруженным условиями `if/else`.
2.  Если перенести логику шансов в `NavigationService`, он перестанет быть просто "двигателем координат" и начнет отвечать за игровую механику (вероятности встреч), что неверно.

### Решение: ExplorationOrchestrator
Вводится новый слой — **ExplorationOrchestrator** (или Facade). Он выступает в роли "Регулировщика".
Его задача — принять намерение игрока (Intent) и решить, что произойдет, делегируя вычисления профильным сервисам.

---

## 2. Архитектура и Поток Данных

Оркестратор становится единой точкой входа для любых действий в мире (перемещение, осмотр, взаимодействие).

### Схема взаимодействия

```mermaid
graph TD
    User[User / Handler] -->|Intent: Move North| Orch[ExplorationOrchestrator]
    
    Orch -->|1. Check Probability| EncService[EncounterService]
    
    EncService -->|Result: EncounterID or None| Orch
    
    Orch -- If Encounter Found -->|2. Save State (Redis)| Redis[(Redis)]
    
    Orch -->|3. Check Type| EncService
    
    EncService -->|Type: Combat or Narrative| Orch
    
    Orch -->|Response DTO| UI[User Interface]
```

### Текстовое представление потока (Детализация)
1.  **User -> Handler:** Игрок нажимает кнопку перемещения.
2.  **Handler -> Orchestrator:** Вызов `handle_move(direction)`.
3.  **Orchestrator -> EncounterService:** Запрос `try_spawn_encounter`.
    *   *Логика:* Проверка вероятности (случайное число) на основе Тира зоны и Навыка игрока.
4.  **Ветвление 1 (Факт встречи):**
    *   **Нет встречи:** Оркестратор вызывает `NavigationService.move()`. Возвращает обновленную карту.
    *   **Встреча есть:**
        *   Оркестратор **сразу** фиксирует состояние `PENDING_ENCOUNTER` в Redis. Это защита от "абуза" (перезагрузки бота, чтобы избежать боя).
        *   Оркестратор получает объект `Encounter` (тип и ID).
5.  **Ветвление 2 (Тип встречи):**
    *   **Тип NARRATIVE (Находка):**
        *   Оркестратор возвращает DTO с описанием события (Сундук, Алтарь) и кнопками действий.
    *   **Тип COMBAT (Бой):**
        *   Оркестратор вызывает `check_detection(user, mob)` для определения условий начала боя.
6.  **Ветвление 3 (Только для Combat - Результат обнаружения):**
    *   **Сценарий А (DETECTED - Игрок заметил):**
        *   **UI:** Показывает сообщение: "Вы заметили [Имя Моба]. Ваши действия?".
        *   **Кнопки:** `[⚔️ Атаковать]` и `[🛡️ Обойти]`.
    *   **Сценарий Б (AMBUSH - Игрока заметили):**
        *   **UI:** Показывает сообщение: "[Имя Моба] выпрыгивает из засады! Бой неизбежен.".
        *   **Кнопки:** Только `[В бой!]`.

---

## 3. Ключевые Аспекты Реализации

### 3.1 Роль ExplorationOrchestrator
*   **Принимает:** `user_id`, `action_type` (move, interact), `params`.
*   **Координирует:**
    1.  Спрашивает `EncounterService`: "Есть ли что-то?"
    2.  **Фиксирует факт встречи в Redis** (чтобы нельзя было сбежать через /start).
    3.  Если это Бой, спрашивает: "Кто кого увидел?"
    4.  Формирует ответ для UI (Карта, Нарратив, Меню выбора или Экран неизбежного боя).
*   **Возвращает:** Универсальный ответ (DTO).

### 3.2 Роль EncounterService
*   **Ответственность:** Чистая математика и генерация случайных чисел.
*   **Входные данные:** `Threat Tier` зоны, Навыки игрока (Survival), контекст (Мир или Рифт).
*   **Логика:**
    *   `try_spawn`: Рассчитывает вероятность встречи (База Тира - Бонус Навыка) и выбирает её тип.
    *   `check_detection`: Сравнивает навык выживания игрока со сложностью (Тиром) моба.
*   **Выходные данные:** Объект `Encounter` (Type, ID) и (опционально) Статус обнаружения.

---

## 4. Планируемые Типы Встреч (Draft)
1.  **Combat (Бой):** Встреча с враждебным существом или группой. Может иметь разные условия старта (Detected/Ambush).
2.  **Narrative (Находка):** Интерактивное событие без прямого боя.
    *   *Примеры:* Заброшенный сундук, Странный алтарь, Труп искателя, Торговец-путешественник.
    *   *Действия:* "Открыть", "Осмотреть", "Пройти мимо".

---

## 5. Механика "Восприятие" (Detection) - Только для Combat

> **Идея:** Использовать навык выживания (Survival / Pathfinder) для получения тактического преимущества.

### Логика работы
1.  **Триггер:** `EncounterService` определил, что тип встречи — **Combat**.
2.  **Проверка:** Сравнивается уровень навыка **Survival (Pathfinder)** игрока и **Тир (Сложность)** моба.

### Сценарии (MVP)
*   **Провал (Низкий навык vs Сильный моб) -> Статус `AMBUSH`**
    *   **Ситуация:** Игрок шумел или был невнимателен. Моб заметил его первым.
    *   **UI:** Экран тревоги. Текст: "Вас заметили!".
    *   **Выбор:** Нет. Игрок обязан принять бой.

*   **Успех (Высокий навык vs Слабый моб) -> Статус `DETECTED`**
    *   **Ситуация:** Игрок двигался тихо и заметил угрозу заранее.
    *   **UI:** Экран выбора. Текст: "Впереди [Моб]. Он вас не видит."
    *   **Выбор:**
        *   `[🛡 Обойти]` -> Игрок успешно избегает боя и **завершает перемещение в целевую клетку**.
            *   *Механика:* Это награда за прокачку. Игрок проходит сквозь опасную зону, не вступая в конфликт.
        *   `[⚔️ Атаковать]` -> Инициация обычного боя.

---

## 6. Архитектура EncounterEngine (Детализация)

Разделение логики на два этапа: "Факт встречи" и "Условия встречи".

### 6.1 Метод `try_spawn_encounter`
*   **Задача:** Определить, произошло ли событие, и какое именно.
*   **Вход:** `loc_data` (флаги зоны, Tier, Context: World/Rift), `user_survival_skill`.
*   **Логика:**
    1.  **Safe Zone Check:** Если `flags['safe_zone'] == True` -> `return None`.
    2.  **Base Chance Lookup:** Получаем базовый шанс из словаря по Тиру зоны.
        *   *Пример:* `{Tier 0: 0.05, Tier 1: 0.15, ..., Tier 7: 1.0}`.
    3.  **Skill Modifier:** `Final Chance = Base Chance - (Survival Skill * 0.01)`.
        *   *Эффект:* Чем выше навык, тем меньше случайных ("мусорных") встреч.
    4.  **Random Check:** Вызов `random.random()`. Если результат > `Final Chance`, то `return None`.
    5.  **Type Selection:** Если встреча состоялась, делаем второй выбор (из таблицы весов для данной зоны):
        *   Например: 70% — Combat, 30% — Narrative.
*   **Выход:** Объект `Encounter` (Type: COMBAT/NARRATIVE, ID: "rat_pack"/"old_chest").

### 6.2 Метод `check_detection`
*   **Задача:** Определить условия начала боя.
*   **Применение:** Вызывается **только** если `try_spawn_encounter` вернул тип `COMBAT`.
*   **Вход:**
    *   `user_skills`: Уровень навыка Survival (Pathfinder).
    *   `mob_tier`: Тир (уровень опасности) моба или группы.
*   **Логика:**
    *   **Формула:** `Detection Score = User.Survival - Mob.Tier`.
    *   **Threshold:** Если `Detection Score >= 0` (или другое пороговое значение), то `DETECTED`. Иначе `AMBUSH`.
    *   *Примечание:* Можно добавить небольшой элемент случайности, чтобы результат не был 100% предсказуемым.
*   **Выход (EncounterState Enum):**
    1.  **AMBUSH:** Навыка не хватило. -> **Бой неизбежен.**
    2.  **DETECTED:** Навык сработал успешно. -> **Выбор: Атака или Обход.**
