# 🛠️ ТЕХНИЧЕСКАЯ СПЕЦИФИКАЦИЯ: АДМИН-ПАНЕЛЬ И DEBUG TOOLS

**Статус:** Planned / Backlog
**Цель:** Инструменты для тестирования баланса, проверки механик и управления состоянием без доступа к SQL-консоли.

---

## 1. Архитектура Админки

Админ-хэндлеры имеют **расширенные права**. Им разрешено:
1.  Игнорировать стандартные UI-сервисы.
2.  Напрямую вызывать `Repositories` для изменения данных (Stat injection, Item generation).
3.  Напрямую вызывать методы `GameService` для запуска механик (Spawn Combat).

**Security:**
*   Фильтр `IsAdmin` (проверка ID из `.env`) обязателен для всех хэндлеров роутера.

---

## 2. Спецификация Команд

### 📦 A. Инвентарь и Экономика

*   **`/give_item <item_key> [quantity]`**
    *   *Описание:* Выдача любого предмета из библиотеки.
    *   *Пример:* `/give_item rusty_sword 1`
    *   *Реализация:* Вызов `InventoryRepo.create_item`.

*   **`/set_currency <type> <amount>`**
    *   *Описание:* Установка баланса валюты.
    *   *Пример:* `/set_currency gold 1000`
    *   *Реализация:* Вызов `WalletRepo.set_balance`.

*   **`/clear_inventory`**
    *   *Описание:* Полная очистка инвентаря (удаление всех предметов). Полезно для тестов "с нуля".

### 💪 B. Персонаж и Статы

*   **`/set_stats <stat_name> <value>`**
    *   *Описание:* Установка базовых характеристик.
    *   *Пример:* `/set_stats strength 150`
    *   *Реализация:* Вызов `CharactersRepo.update_stats`.

*   **`/heal`**
    *   *Описание:* Полное восстановление HP и Энергии.

*   **`/reset_cooldowns`**
    *   *Описание:* Сброс всех кулдаунов способностей и предметов.

### 🌍 C. Мир и Навигация

*   **`/teleport <x> <y>`**
    *   *Описание:* Мгновенное перемещение в любую точку карты.
    *   *Пример:* `/teleport 52 52` (в Хаб).
    *   *Реализация:* Обновление координат в БД и сброс кэша навигации.

*   **`/regen_zone <zone_id>`**
    *   *Описание:* Принудительный запуск `ZoneOrchestrator` для перегенерации описания и контента зоны.
    *   *Пример:* `/regen_zone D4_1_1`

*   **`/set_threat <x> <y> <value>`**
    *   *Описание:* Принудительное изменение уровня угрозы в точке (для теста спавна мобов разных тиров).

### ⚔️ D. Боевая Система (`/debug_combat`)

Мгновенный запуск боя с манекеном или копией игрока, минуя очереди матчмейкинга.

*   **Синтаксис:** `/debug_combat [mode]`
*   **Пример:** `/debug_combat` (стандартный манекен) или `/debug_combat shadow` (копия себя).
*   **Реализация:**
    1.  Получить `ArenaService`.
    2.  Вызвать метод `create_shadow_battle(mode="pve")`.
    3.  Принудительно перевести FSM админа в `InGame.combat`.

### 🔧 E. Системные

*   **`/reset_fsm`**
    *   *Описание:* Сброс состояния FSM в `default`. Полезно, если игрок "застрял" в меню.

*   **`/flush_redis`**
    *   *Описание:* Полный сброс кэша Redis.

---

## 3. Внешние Скрипты (External Scripts)

В папке `scripts/` находятся мощные инструменты для глубокой отладки, которые запускаются из консоли сервера.

1.  **`debug_read_db.py`**
    *   Универсальный инспектор БД.
    *   Позволяет просматривать состояние любой клетки мира, статистику регионов и выполнять сырые SQL-запросы.
    *   Умеет экспортировать данные в файлы.

2.  **`seed_world_gen.py`**
    *   Генератор мира.
    *   Используется для первичного создания карты, расстановки Якорей и генерации контента через LLM.
