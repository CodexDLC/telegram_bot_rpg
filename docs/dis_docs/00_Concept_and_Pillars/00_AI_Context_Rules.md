# 🤖 AI Context & Rules (Правила для Ассистента)

Этот документ регулирует, как AI-ассистент должен интерпретировать файлы проекта, документацию и текущий код.

## 1. Статус Документации (`docs/`)
* **Концепты, а не ТЗ:** Файлы в папке `docs/` (особенно `idea/`) содержат **видение будущего** и **архив идей**.
* **НЕ БАГИ:** Отсутствие реализации фичи, описанной в документации, **НЕ ЯВЛЯЕТСЯ ошибкой**, пока разработчик прямо не заявил, что эта фича должна работать сейчас.
* **Приоритет:** Документация описывает "Идеальное состояние" (Goal). Код описывает "Текущее состояние" (Current State). Разрыв между ними — это нормальный рабочий процесс, а не "критический долг".

## 2. Разрешение Конфликтов (Code vs Docs)
Если реализация в коде (`.py`) противоречит описанию в документации (`.md`):
1.  **НЕ КРИТИКОВАТЬ СРАЗУ.** Не пиши: "Твой код неправильный, потому что в доках не так".
2.  **УТОЧНИТЬ ИСТИНУ.** Задай вопрос: *"Я вижу расхождение между кодом (реализовано X) и доками (описано Y). Мы изменили концепцию и код сейчас является истиной, или это временное решение / ошибка?"*
3.  **Адаптация:**
    * Если *Код* = Истина: Предложи обновить документацию.
    * Если *Доки* = Истина: Предложи план рефакторинга кода.

## 3. Режим "Ментора"
* **Фокус:** Безопасность, Архитектура, Чистота кода.
* **Запрет:** Не навязывать реализацию фич из `docs/`, если текущая задача касается другого модуля.
* **Контекст:** Всегда помнить, что проект находится в стадии **Pre-Alpha**. Многие механики — заглушки (stubs).