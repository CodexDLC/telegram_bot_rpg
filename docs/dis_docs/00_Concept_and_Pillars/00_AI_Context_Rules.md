# 🤖 AI CONTEXT & RULES (ПРАВИЛА ДЛЯ АССИСТЕНТА)

Этот документ регулирует, как AI-ассистент должен интерпретировать файлы проекта, документацию и текущий код.

---

## 1. Ключевые Источники Истины (Sources of Truth)

При поиске ответов на вопросы, в первую очередь следует опираться на следующие документы:

*   **ЛОР и Сеттинг:** `docs/dis_docs/03_World_and_Lore/00_World_Bible.md`
    *   Это главный каноничный документ по миру игры. Все остальные документы должны ему соответствовать.
*   **Архитектура и Структура:** `docs/dis_docs/04_Technical_Specs/01_Architecture_and_Stack.md`
    *   Этот документ описывает актуальную структуру папок и поток данных в приложении.

---

## 2. Статус Документации (`docs/`)

*   **Концепты, а не ТЗ:** Файлы в папке `docs/` описывают **видение** и **цели**. Отсутствие реализации фичи, описанной в документации, **НЕ ЯВЛЯЕТСЯ ошибкой**, пока разработчик прямо не заявил, что эта фича должна работать сейчас.
*   **Приоритет:** Документация описывает "Идеальное состояние" (Goal). Код описывает "Текущее состояние" (Current State). Разрыв между ними — это нормальный рабочий процесс.
*   **Рабочие Черновики (Drafts):** Некоторые документы могут иметь статус "DRAFT" или "PLANNED" (например, `02_Death_and_Save_System.md`). Это означает, что механика еще не утверждена, и по ней нужно задавать больше уточняющих вопросов, а не принимать как финальное ТЗ.

---

## 3. Разрешение Конфликтов (Code vs Docs)

Если реализация в коде (`.py`) противоречит описанию в документации (`.md`):

1.  **НЕ КРИТИКОВАТЬ СРАЗУ.** Не пиши: "Твой код неправильный, потому что в доках не так".
2.  **УТОЧНИТЬ ИСТИНУ.** Задай вопрос: *"Я вижу расхождение между кодом (реализовано X) и доками (описано Y). Мы изменили концепцию и код сейчас является истиной, или это временное решение / ошибка?"*
3.  **Пример Правильного Подхода:**
    *   *Ситуация:* Разработчик помнил, что `INFLUENCE_TAGS` в конфиге имели уровни (0,1,2,3), а в коде были диапазоны (1,2), (3,4).
    *   *Действие:* Вместо того чтобы сразу менять код, ассистент проверил Git-историю, не нашел старой версии и уточнил у разработчика. Выяснилось, что код является истиной. Это правильный подход.
4.  **Адаптация:**
    *   Если **Код = Истина**: Предложи обновить документацию.
    *   Если **Доки = Истина**: Предложи план рефакторинга кода.

---

## 4. Режим "Ментора"

*   **Фокус:** Безопасность, Архитектура, Чистота кода.
*   **Запрет:** Не навязывать реализацию фич из `docs/`, если текущая задача касается другого модуля.
*   **Контекст:** Всегда помнить, что проект находится в стадии **Pre-Alpha**. Многие механики — заглушки (stubs).
