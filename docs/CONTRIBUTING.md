
# Руководство для разработчика (Contributing Guide)

Добро пожаловать в команду разработки **Telegram Bot RPG**!
Этот документ описывает процессы разработки, стандарты кода и правила взаимодействия. Следование этим правилам обязательно для поддержания качества и стабильности проекта.

---

## 📋 Оглавление

1. [Подготовка окружения](#1-подготовка-окружения)
2. [Процесс разработки (Git Flow)](#2-процесс-разработки-git-flow)
3. [Стандарты кода](#3-стандарты-кода)
4. [Создание Pull Request (PR)](#4-создание-pull-request-pr)
5. [Полезные команды](#5-полезные-команды)

---

## 1. Подготовка окружения

Мы используем Python 3.11+. Убедитесь, что у вас установлена эта версия.

### Шаг 1. Клонирование репозитория
```bash
git clone <ссылка_на_репозиторий>
cd telegram_bot_rpg
````

### Шаг 2. Создание виртуального окружения (venv)

Рекомендуется создавать venv в папке `.venv` (она уже добавлена в `.gitignore`).

**Windows:**

Bash

```
python -m venv .venv
.venv\Scripts\activate
```

**Linux / macOS:**

Bash

```
python3 -m venv .venv
source .venv/bin/activate
```

### Шаг 3. Установка зависимостей

Мы разделяем зависимости на _боевые_ (для запуска бота) и _инструментальные_ (для разработки: линтеры, тесты).

Bash

```
pip install -r requirements-dev.txt
```

_(Примечание: `requirements-dev.txt` автоматически установит и `requirements.txt`)._

### Шаг 4. Настройка переменных окружения

1. Найдите файл `.env.example`.
    
2. Создайте его копию и назовите её `.env`.
    
3. Заполните `.env` вашими локальными токенами и доступами.
    
    > ⚠️ **Важно:** Никогда не коммите файл `.env` в репозиторий!
    

---

## 2. Процесс разработки (Git Flow)

Мы используем упрощенную модель **Git Flow**.

### Основные ветки:

- `main` — **Production**. Стабильный код, который находится на сервере. Прямые коммиты запрещены.
    
- `develop` — **Pre-Production**. Основная ветка разработки. Сюда сливаются все задачи.
    

### Правила работы:

1. **Всегда** начинайте работу с обновления ветки `develop`:
    
    Bash
    
    ```
    git checkout develop
    git pull origin develop
    ```
    
2. **Создавайте новую ветку** под каждую задачу. Называйте ветки осмысленно:
    
    - `feature/new-mechanic` — для новой функциональности.
        
    - `fix/bug-name` — для исправления ошибок.
        
    - `refactor/component-name` — для улучшения кода без изменения логики.
        
    
    Bash
    
    ```
    git checkout -b feature/auth-system
    ```
    
3. **Коммиты:**
    
    - Пишите сообщения коммитов на понятном языке (можно на русском или английском, главное — единообразие).
        
    - Пример: `feat: добавлена регистрация пользователя` или `fix: исправлена ошибка в расчете урона`.
        

---

## 3. Стандарты кода

Мы строго следим за качеством кода. Конфигурация линтеров находится в `pyproject.toml`.

### Инструменты:

- **Ruff:** Используется для линтинга (поиск ошибок) и форматирования.
    
- **Mypy:** Используется для строгой статической типизации.
    

### Перед коммитом:

Вы **обязаны** запустить локальную проверку. Код, не проходящий проверки, не будет принят.

Bash

```
# 1. Проверка стиля и ошибок
ruff check .

# 2. Проверка типов
mypy .

# 3. Запуск тестов (если есть)
pytest
```

---

## 4. Создание Pull Request (PR)

Когда задача готова:

1. Запушьте вашу ветку:
    
    Bash
    
    ```
    git push origin feature/auth-system
    ```
    
2. Зайдите на GitHub/GitLab и создайте **Pull Request** (Merge Request).
    
    - **Base branch:** `develop`
        
    - **Compare branch:** ваша ветка (`feature/auth-system`)
        
3. В описании PR кратко расскажите, что было сделано.
    
4. Дождитесь прохождения автоматических тестов (CI) и ревью от другого разработчика.
    
5. После одобрения ваш код будет слит в `develop`.
    

---

## 5. Полезные команды

- **Обновить зависимости:** Если в `requirements.txt` что-то изменилось:
    
    Bash
    
    ```
    pip install -r requirements-dev.txt
    ```
    
- **Очистить кэш Python (если странные ошибки):** Windows: `rd /s /q __pycache__` (в папках проекта) Linux/Mac: `find . -name "__pycache__" -exec rm -rf {} +`
    



---

### 3. Настройка веток (прямо сейчас)

Ты — "DevOps" на сегодня. Давай настроим репозиторий правильно.

Предполагаем, что ты сейчас находишься в ветке `main` (или `master`) и у тебя там актуальный код.

**Введи эти команды в терминале по очереди:**

1.  **Убедись, что всё закоммичено:**
    ```bash
    git add .
    git commit -m "Setup: added CONTRIBUTING.md and CI config"
    ```
    *(Сделай это после того, как добавишь файл CONTRIBUTING.md и папку .github)*

2.  **Создай ветку `develop`:**
    ```bash
    git checkout -b develop
    ```
    *Теперь ты находишься в ветке `develop`. Она точная копия `main`.*

3.  **Запушь `develop` на сервер:**
    ```bash
    git push -u origin develop
    ```

4.  **Начни "разработку" (создай ветку под текущую задачу):**
    Допустим, мы хотим внедрить "Имя Симбиота" или продолжить настройку. Создаем тематическую ветку ОТ `develop`.
    ```bash
    git checkout -b feature/project-structure
    ```

**Итог:**
1.  Твой `main` остался чистым (как бэкап).
2.  Вся работа сейчас идет в `feature/project-structure`.
3.  Когда мы закончим на сегодня, ты сольешь `feature/project-structure` в `develop`.

Теперь ты можешь спокойно работать. Когда будешь кидать мне файлы, просто знай, что для меня это просто текст, а для Git'а — история изменений в твоей новой ветке.
