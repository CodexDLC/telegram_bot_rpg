# 🧩 Aggregators (Assemblers)

[⬅️ Назад: Specs](../README.md)

---

## 🎯 Описание
Агрегаторы — это "рабочие лошадки", которые превращают сырые данные из БД в готовый `TempContext`.

## 🏗️ Реализации

### 1. PlayerAssembler
Собирает данные игрока.
*   **Input:** `CharacterModel`, `InventoryModel[]`, `SkillModel[]`.
*   **Logic:**
    *   Считает статы от атрибутов.
    *   Применяет бонусы от экипировки.
    *   Формирует `math_model` для боя.

### 2. MonsterAssembler
Собирает данные монстра.
*   **Input:** `MonsterTemplate` (JSON).
*   **Logic:**
    *   Скалирует статы под уровень.
    *   Генерирует лут (виртуально).
    *   Применяет аффиксы (Elite, Boss).
