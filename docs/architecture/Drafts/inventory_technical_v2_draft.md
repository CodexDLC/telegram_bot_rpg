# 🏗️ Технические решения: Инвентарь и Предметы (Draft v2.0)

## 1. Многослойная защита (Armor & Clothing)
*   **Логика:** Система поддерживает одновременную экипировку в слоты `Armor` и `Clothing`.
*   **Обработка:** Типы защиты (физическая, энергетическая и т.д.) у этих слоев разные. Они не суммируются напрямую и не конфликтуют между собой. Каждый слой обрабатывает входящий урон согласно своим коэффициентам.

## 2. Хранилище ресурсов (Resource Wallet)
*   **MVP:** Реализовано как бездонное хранилище (infinite capacity) для упрощения логики на старте.
*   **Перспектива:** Предусмотреть слот под артефакт «Пространственный карман», который будет управлять лимитами этого слоя.

## 3. Быстрые слоты (Quick Slots)
*   **Зависимость:** Наличие и количество доступных `active_slots` в бою жестко завязано на наличие предмета в слоте `BELT`.
*   **Валидация:** Если `char.equipment.belt is None`, список доступных расходников в боевом интерфейсе должен быть пуст.

## 4. Персистентность «Грязных» ресурсов
*   **Runtime:** Грязные ресурсы хранятся в ключе игрока в Redis (`ac:{char_id}:unsecured_resources`).
*   **Persistence:** Для синхронизации и бекапов в таблицу ресурсов БД добавляется поле `unsecured_json` (тип JSONB). Это позволяет восстановить состояние «грязного» инвентаря при сбоях или стабилизации сессии.

## 5. Идентификация предметов
*   **Уникальность:** Каждому предмету при материализации (спавн в инвентарь игрока) присваивается уникальный UUID. Даже идентичные базовые предметы (например, два "Старых меча") являются разными сущностями с собственными ID.

## 6. Теги компонентов
*   **Назначение:** Теги (например, `sharp`, `magic_infused`) не выводятся игроку напрямую. Они используются внутренними сервисами:
    *   **Dismantle Service:** Для определения состава ресурсов при разборе.
    *   **LLM Content Service:** Как вводные данные (prompts) для генерации уникальных названий и лорных описаний созданных предметов.