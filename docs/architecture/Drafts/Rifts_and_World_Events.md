# 🏗️ TECHNICAL SPECIFICATION: RIFTS & EXPLORATION UPDATE

> **Status:** Draft / Ready for Implementation
> **Target Phase:** Phase 3 (Gameplay Loop)
> **Note:** Post-Refactoring Idea. Реализация отложена до стабилизации ядра v3.1.

## 1. Концепция: Рифты (Reality Shards)
Рифты — это процедурно генерируемые инстансы (графы), существующие параллельно с основным миром. Они строятся по принципу "Шаблон (Template) -> Экземпляр (Instance)" для оптимизации ресурсов и реализации механики "Первопроходца".

### 1.1. Архитектура Данных (Database)
Мы используем гибридный подход: реляционные связи для структуры и JSONB для гибкости контента.

#### A. Таблица `game_rift_templates` (Шаблоны)
Хранит сгенерированную "карту" и "легенду". Создается один раз, используется многими игроками.

*   `id` (String Hash): Уникальный ключ, генерируемый из `Biome + Tier + NarrativeTags + TopologySeed`.
*   `structure_graph` (JSONB):
    *   Топология (Nodes & Edges).
    *   Типы комнат (`combat`, `treasure`, `mining`, `boss`).
    *   Логические теги для генератора (`tag:wet`, `tag:narrow`).
    *   *В шаблоне НЕТ состояния (открытых сундуков, убитых мобов).*
*   `narrative_content` (JSONB):
    *   Словарь: `{ "node_id": { "name": "...", "description": "..." } }`.
    *   Генерируется LLM "лениво" или при создании (для Первопроходца).
*   `pioneer_id` (FK): ID игрока, для которого этот шаблон был создан впервые.

#### B. Таблица `game_rift_instances` (Инстансы)
Хранит личный прогресс игрока в конкретном шаблоне.

*   `id` (UUID): Уникальный ID текущего похода.
*   `template_id` (FK): Ссылка на используемый Шаблон.
*   `user_id` (FK): Владелец инстанса.
*   `state_data` (JSONB): Динамический слой изменений.
    *   `nodes_visited`: Список ID посещенных комнат.
    *   `objects_interaction`: `{ "chest_node_5": "looted", "door_node_2": "opened" }`.
    *   `mobs_killed`: Список ID убитых групп.

#### C. Таблица `game_rift_history` (Журнал)
Many-to-Many связь для механики "Smart Reuse".

*   `user_id` + `template_id`: Запись о том, что игрок уже видел этот сюжет.
*   Используется для исключения повторов при поиске шаблона.

### 1.2. Алгоритм Генерации: "Organic Growth" & Tags
Генератор не использует жесткие пресеты ("Квадрат", "Линия"). Он выращивает граф.

*   **Input:** Тир (0-8), Биом (Лес, Руины...), Нарративные Теги (Мрачно, Техногенно, Слизь).
*   **Growth Loop (Рост):**
    1.  Старт с `Entry Node`.
    2.  Цикл добавления нод с вероятностью ветвления.
    3.  **Looping:** 10-15% шанс, что новая нода соединится с уже существующей (создавая петлю), а не создаст новую.
*   **Content Injection (Наполнение):**
    *   Определение тупиков -> `Treasure` / `Elite Mob`.
    *   Определение узких мест (бутылочных горлышек) -> `Gatekeeper` / `Trap`.
    *   Самая дальняя точка -> `Core Node` (Босс + Ядро).
*   **Narrative Hash:** Сборка всех тегов в единый промпт для LLM, чтобы получить целостное описание ("Техногенный лес в слизи").

---

## 2. Геймплейный Цикл: Экспедиция и Ядро
Рифт — это не только бой, но и добыча (Extraction Loop).

### 2.1. Ресурсы и Майнинг
В графе шаблона есть точки `resource_vein` (Жилы).
*   Игрок может зачистить рифт в боевой экипировке, выйти, переодеться в "грузчика" (макс. слотов) и вернуться копать.
*   Состояние жил (`depleted: true`) пишется в `state_data` инстанса.

### 2.2. Механика Ядра (The Core)
Главная цель — Ядро Рифта (предмет/валюта для прокачки Симбиота).
*   Находится в финальной комнате.
*   **Collapse Event:** Извлечение ядра запускает таймер уничтожения инстанса (или мгновенно закрывает доступ).
*   При коллапсе: удаляется запись из Redis, ставится статус `closed` в Postgres.

---

## 3. Система Энкаунтеров (Exploration Engine)
Логика, работающая при перемещении по Миру (не в Рифте).

### 3.1. Two-Step Verification (Двухэтапная проверка)
**Шаг 1: Spawn Check (Проверка вероятности встречи)**
*   **Вход:** Location Tier, Location Flags (Safe Zone?), Global Modifiers.
*   **Логика:** Генерация случайного числа (`random.random()`) и сравнение с базовым шансом, зависящим от Тира локации.
*   **Результат:** `Mob Pack ID` или `None`.

**Шаг 2: Initiative Check (Определение инициативы)**
*   **Вход:** Player Perception/Stealth vs Mob Perception/Stealth.
*   **Результат (State):**
    *   🔴 **AMBUSH:** Игрок не заметил, Моб заметил. (Бой сразу, стан/дебафф).
    *   🟡 **NORMAL:** Оба заметили. (Стандартный бой).
    *   🟢 **DETECTED:** Игрок заметил, Моб нет. (Меню: Напасть / Обойти / Изучить).

### 3.2. Механика "Rift Leak" (Протечка)
При генерации энкаунтера система проверяет `Active Rifts` в радиусе X клеток.
*   Если рядом есть Рифт, с шансом 30% в пул монстров подмешиваются уникальные мобы этого Рифта.
*   **Нарратив:** "Вы видите кристальных пауков... значит, Кристальный Разлом где-то рядом".

---

## 4. Монстры и Кланы (Granular Tier System)
Система, позволяющая использовать одни и те же ассеты (Кланы) на разных уровнях сложности без потери логики.

### 4.1. Структура `MonsterFamily` и `MonsterVariant`
*   **`MonsterFamily`** (Клан, например, "Бандиты") — это коллекция юнитов.
*   **`MonsterVariant`** (Юнит) — это конкретный тип монстра внутри семьи. Каждый вариант имеет свой диапазон эффективности, заданный полями `min_tier` и `max_tier`.

    *   *Пример для семьи "Wasteland Scavengers":*
        *   `rat_variant` (роль `minion`): `min_tier: 0`, `max_tier: 2`.
        *   `thug_variant` (роль `elite`): `min_tier: 1`, `max_tier: 4`.
        *   `warlord_variant` (роль `boss`): `min_tier: 4`, `max_tier: 6`.

### 4.2. `ClanFactory` (Фабрика Кланов)
Фабрика отвечает за сборку боевых групп (`паков`) для конкретного рифта.

*   **Сценарий:** Запрос "Собрать пак `Scavengers` для Рифта **Тир 5**".
    1.  **Фильтрация:** Фабрика просматривает все `variants` в семье "Scavengers".
        *   `rat_variant` отсеивается (его `max_tier` < 5).
        *   `thg_variant` отсеивается (его `max_tier` < 5).
        *   `warlord_variant` подходит (5 находится в диапазоне 4-6).
    2.  **Сборка Пака:** Фабрика пытается собрать валидную группу из подходящих юнитов. Например, для комнаты босса она обязательно возьмет `warlord_variant`. Для обычных комнат она может взять юнитов, чей тир немного ниже, чтобы создать "свиту".
    3.  **Исключение:** Если из доступных юнитов невозможно собрать логичную группу (например, для рифта Тир 1 у клана есть только юниты с `min_tier: 3`), то этот клан исключается из выборки для данного рифта.
