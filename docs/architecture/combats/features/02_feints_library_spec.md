# Feints Library Specification (RBC v3.1)

Финты (Feints) — это специальные приемы, которые становятся доступны игроку при накоплении тактических токенов.
Это не просто "кнопки урона", а контекстные действия, зависящие от оружия, стойки и типа врага.

---

## 1. Структура Данных (Data Model)
Библиотека финтов разделена на два слоя: **Logic DB** (для Калькулятора) и **Text DB** (для UI/Logs).

### A. Logic DB (Mechanics)
Определяет стоимость, условия и влияние на пайплайн.
**Важно:** Все флаги — это **Boolean Switches**. Никаких цифр. Цифры берутся из статов актера.

**Типы Флагов:**
*   `check_*` (bool): Включить проверку (например, `check_stun`).
*   `force_*` (bool): Принудительный успех (например, `force_crit`).
*   `trigger_*` (bool): Использовать альтернативную логику/формулу (например, `trigger_crit_power`).
*   `ignore_*` (bool): Игнорировать защиту (например, `ignore_armor`).

```python
"head_attack": {
    "cost": {"tactics": 1},
    "variants": {
        "swords": {
            "trigger": "CRITICAL_BLEED", 
            "flags": {"force_crit": True} # Гарантированный крит (если триггер сработал)
        },
        "macing": {
            "trigger": "CONCUSSION", 
            "flags": {"check_stun": True} # Включить проверку стана
        },
        "fencing": {
            "trigger": "FATAL_POINT", 
            "flags": {"ignore_armor": True, "force_crit": True}
        },
        "default": {
            "trigger": "HEAD_HIT",
            "flags": {"trigger_crit_power": True} # Использовать усиленную формулу крита
        }
    }
}
```

### B. Text DB (Narrative)
Определяет, что видит игрок. Текст зависит от контекста (оружие + тип врага).

```python
"head_attack": {
    "button": "💀 В голову",
    "swords": {
        "humanoid": {
            "action": "пытается нанести рубящий удар в голову",
            "success": "лезвие находит брешь в шлеме",
            "fail": "клинок соскальзывает с шлема"
        },
        "beast": {
            "action": "опускает клинок на череп зверя",
            "success": "рассекает морду чудовища",
            "fail": "зверь успевает отдернуть голову"
        }
    },
    "macing": {
        "humanoid": {
            "action": "обрушивает молот на шлем",
            "success": "сминает шлем вместе с головой",
            "fail": "удар приходится вскользь"
        }
    }
}
```

---

## 2. Типы Врагов (Target Anatomy)
Система должна знать анатомию цели, чтобы фильтровать недоступные финты.

*   **Humanoid:** (Голова, Тело, Руки, Ноги). Полный набор.
*   **Beast:** (Голова, Тело, Лапы). Нет "Рук" (Disarm работает иначе).
*   **Serpentine:** (Голова, Тело, Хвост). Нет "Ног" (Подсечка не работает).
*   **Flying:** (Голова, Тело, Крылья). Спец-атаки по крыльям (сбитие).
*   **Amorphous:** (Ядро). Нет зон. Только общие атаки.

---

## 3. Примеры Финтов (Draft List)

### Зона: Голова (Head)
*   **Headshot (Swords/Fencing):** Повышенный крит / Кровотечение.
*   **Concussion (Macing):** Шанс оглушения (Stun).
*   **Blinding (Daggers):** Ослепление (Force Miss врагу).

### Зона: Ноги (Legs)
*   **Leg Sweep (Polearms/Staff):** Сбитие с ног (Knockdown).
*   **Hamstring (Daggers):** Замедление (Slow) / Снижение уворота.
*   **Kneecap (Macing):** Перелом (Trauma).

### Зона: Руки (Arms)
*   **Disarm (Fencing):** Выбивание оружия (Disarm).
*   **Arm Break (Macing):** Снижение точности / урона врага.

### Беззонные (General)
*   **Shield Bash (Shield):** Урон + Отталкивание.
*   **Whirlwind (2H Swords):** AOE атака.
*   **Flurry (Dual Wield):** Серия быстрых ударов.

---

## 4. Timing (Instant vs Action)
Финты могут быть двух типов:

1.  **Instant (Мгновенные):**
    *   Не тратят ход.
    *   Применяются сразу (в ветке `_process_unidirectional`).
    *   Пример: "Бросок песка" (Дебафф), "Боевой клич" (Бафф).

2.  **Action (Атакующие):**
    *   Тратят ход (являются модификатором атаки).
    *   Участвуют в обмене (ветка `_process_exchange`).
    *   Пример: "Удар щитом" (Атака с параметрами оружия + Стан).