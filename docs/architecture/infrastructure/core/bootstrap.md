# 🚀 Bootstrap & Initialization

⬅️ [Назад](./README.md)

## 🤖 Bot Factory
> **Source:** `apps/common/core/bot_factory.py`

Функция `build_app(container)`:
1.  Проверяет наличие токенов.
2.  Проверяет соединение с Redis (Ping).
3.  Создает `RedisStorage` для FSM.
4.  Инициализирует `Dispatcher` и подключает `DbSessionMiddleware`.

## ⚡ ARQ Workers
> **Source:** `apps/common/core/base_arq.py`

Базовый класс `BaseArqSettings` обеспечивает единую конфигурацию для всех воркеров:
*   Создание пула Redis при старте (`on_startup`).
*   Закрытие соединений при остановке (`on_shutdown`).
*   Настройки таймаутов и повторных попыток.
