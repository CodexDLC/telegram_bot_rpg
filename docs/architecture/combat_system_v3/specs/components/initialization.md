# Component: CombatEntryOrchestrator (Initialization)

⬅️ [Назад](../README.md) | 🏠 [Документация](../../../../README.md)

**File:** `apps/game_core/modules/combat/combat_entry_orchestrator.py`
**Layer:** API Layer (Start).
**Responsibility:** Создание и настройка боевой сессии.

## 1. Сценарии Входа

### A. Standard PvE (`standard_pve`)
*   **Вход:** Список команд (`teams`).
*   **Логика:**
    1.  Парсит команды, назначает цвета (blue, red).
    2.  Собирает список всех участников (игроков и монстров).
    3.  Запрашивает данные у `ContextAssembler`.
    4.  Создает сессию.

### B. Shadow Duel (`shadow_duel`)
*   **Вход:** `char_id` игрока.
*   **Логика:**
    1.  Создает команду `blue` с игроком.
    2.  Создает команду `red` с теневой копией игрока (ID = `-char_id`).
    3.  Использует одни и те же данные (`ref`) для обоих.

### C. Arena Match (`arena_match`)
*   **Вход:** Готовый конфиг команд от матчмейкера Арены.
*   **Логика:** Аналогична Standard PvE, но без рандома цветов.

---

## 2. Процесс Создания (Pipeline)

### Шаг 1: Assemble (Сбор Данных)
Оркестратор не лезет в БД сам. Он отправляет запрос в `ContextAssembler` (через `CoreRouter`).
*   **Request:** `ContextRequestDTO(player_ids, monster_ids)`.
*   **Response:** Маппинг `ID -> RedisKey`. Ассемблер сам загружает данные из SQL в Redis (`:raw`, `:loadout`).

### Шаг 2: Build (Настройка Сессии)
Вызывает `CombatLifecycleService.create_battle`.
*   Генерирует `session_id`.
*   Создает структуру `:meta` и `:targets`.
*   Перемешивает цели (Shuffle), чтобы враги не фокусили одного игрока.

### Шаг 3: Link (Привязка)
Вызывает `CombatSessionService.link_players_to_session`.
*   Записывает `session_id` в профиль игрока (`accounts:map`), чтобы он мог вернуться в бой при рестарте бота.
