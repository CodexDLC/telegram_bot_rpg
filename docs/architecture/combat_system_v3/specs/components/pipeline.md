# Component: CombatPipeline

⬅️ [Назад](../README.md) | 🏠 [Документация](../../../../README.md)

**Concept:** Алгоритм обработки одного атомарного взаимодействия (A бьет B).
**Implementation:** Последовательный вызов сервисов внутри цикла `Executor`.

---

## 📑 Фазы Пайплайна

### Pre-Pipeline: Analysis (Executor Level)
Перед запуском пайплайна `Executor` вызывает `ContextBuilder.analyze_exchange`.
*   **Цель:** Определить количество задач (Dual Wield) и конфликты (Interference).
*   **Результат:** Словари `external_mods`, которые передаются в пайплайн.

### Фаза 0: Context Build (ContextBuilder)
*   **Цель:** Создать "пульт управления" (`PipelineContextDTO`).
*   **Действия:**
    *   Применить `external_mods` (отключить атаку, переключить руку).
    *   Собрать флаги из Интента (Skill/Item).

### Фаза 1: Pre-Calculation (AbilityService)
*   **Цель:** Подготовить почву.
*   **Действия:**
    *   Проверить активные эффекты.
    *   Впрыснуть временные статы в `temp` (Instant Mods).

### Фаза 1.5: Stats Calculation (StatsEngine)
*   **Цель:** Актуализировать цифры.
*   **Действия:**
    *   Проверить `dirty_stats`.
    *   Пересчитать `ActorStats` (Waterfall).

### Фаза 1.6: Liveness Check
*   **Цель:** Проверка валидности участников.
*   **Действия:**
    *   Если Атакующий мертв -> Прервать.

### Фаза 2: Combat Calculator (CombatResolver)
*   **Цель:** Математический расчет.
*   **Действия:**
    *   Accuracy -> Evasion -> Parry -> Block -> Damage.
*   **Результат:** `InteractionResultDTO`.

### Фаза 3: Post-Calculation (AbilityService)
*   **Цель:** Реакция на результат.
*   **Действия:**
    *   Проверить триггеры (On Hit, On Crit).
    *   Наложить эффекты.

### Фаза 4: Mechanics (MechanicsService)
*   **Цель:** Применение изменений (Commit).
*   **Действия:**
    *   `HP -= Damage`.
    *   `Tokens += 1`.
    *   `XP += Event`.
