# Component: AbilityService

⬅️ [Назад](../README.md) | 🏠 [Документация](../../../../README.md)

**File:** `apps/game_core/modules/combat/services/ability_service.py`
**Responsibility:** Управление жизненным циклом абилок, эффектов и временных модификаторов.

## 1. Концепция
`AbilityService` — это "мозг" управления эффектами. Он вмешивается в процесс расчета боя на двух этапах: ДО (Pre-Calc) и ПОСЛЕ (Post-Calc), а также следит за длительностью эффектов.

Он работает с `PipelineContextDTO`, модифицируя его состояние.

---

## 2. Этапы работы (Pipeline Integration)

### A. Pre-Calculation (Подготовка)
Вызывается перед запуском `CombatResolver`.

**Вход:** `PipelineContextDTO` (с флагами и статами), `ActionPayload` (какой скилл используется).

**Логика:**
1.  **Анализ Действия:** Смотрит, какой скилл/финт применяется.
2.  **Анализ Активных Эффектов:** Проверяет, есть ли на персонаже баффы/дебаффы, влияющие на атаку.
3.  **Вмешательство (Temp Injection):**
    *   Если нужно изменить статы (например, +50% урона), сервис пишет инструкцию в `actor.raw.modifiers.{stat}.temp`.
    *   **Dirty Flags:** При изменении `temp`, сервис добавляет имя измененного стата в `actor.dirty_stats`.
    *   *Важно:* Если `actor.stats` (кэш) отсутствует, помечать флаги не нужно — будет полный пересчет.
4.  **Установка Флагов:** Может включить/выключить флаги в `ctx.flags` (например, `force.crit = True`).
5.  **Регистрация Триггеров:** Записывает в `ctx.triggers` инструкции для Post-Calc (например, `{"on_hit": "apply_bleed"}`).

### B. Post-Calculation (Реакция)
Вызывается после получения результата от `CombatResolver`.

**Вход:** `InteractionResultDTO`, `PipelineContextDTO`.

**Логика:**
1.  **Чтение Триггеров:** Смотрит в `ctx.triggers`, какие инструкции были записаны.
2.  **Проверка Условий:** Сверяет результат (`is_hit`, `is_crit`) с условиями триггера.
3.  **Выполнение:**
    *   Если условие выполнено -> Вызывает метод наложения эффекта (`apply_effect`).
    *   Пример: "Если был Hit -> Наложить эффект Bleed на 3 хода".

### C. Lifecycle Management (Тики)
Вызывается в начале хода (или в конце, при очистке).

**Логика:**
1.  **Проверка Таймеров:** Проходит по списку `active_abilities`.
2.  **Снятие:** Если время действия истекло -> Удаляет эффект и связанные с ним модификаторы статов из `temp`.
    *   При удалении также обновляет `dirty_stats`.
3.  **Периодический Эффект:** Если это DoT/HoT -> Рассчитывает и применяет эффект (через `MechanicsService`).

---

## 3. Структура Эффекта (Ability Config)

Каждая абилка/эффект описывается словарем (или DTO), который знает:
*   **Duration:** Длительность (в разменах).
*   **Impact:** Мгновенный эффект (урон/хил).
*   **Modifiers:** Влияние на статы (словарь для `temp`).
*   **Triggers:** Условия срабатывания доп. эффектов.
