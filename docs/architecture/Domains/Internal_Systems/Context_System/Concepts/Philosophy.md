# 💡 Philosophy v2

[⬅️ Назад: Concepts](README.md)

---

## Проблема v1 (Monolith)
Старый сборщик контекста был жестко привязан к бою.
*   **Всегда грузил всё:** Даже если нужен только HP, он грузил инвентарь, скиллы и квесты.
*   **Тяжелые запросы:** Bulk-запросы к БД были огромными и медленными.
*   **Нет гибкости:** Нельзя было использовать его для отрисовки меню инвентаря.

## Решение v2 (Modular Aggregator)
Мы превратили его в универсальный сервис.

### 1. Scope-Based Loading
Мы ввели понятие **Scope** (Область видимости).
*   `scope="combats"` -> Грузим всё для боя.
*   `scope="status"` -> Грузим только статы.
*   `scope="inventory"` -> Грузим только предметы.

### 2. Query Planner
Вместо одного огромного SQL-запроса, мы строим план запроса динамически.
Если в scope нет инвентаря — мы даже не трогаем таблицу `inventory_items`.

### 3. Temp Context Hierarchy
Вместо одного DTO на все случаи жизни, мы сделали иерархию:
*   `CombatTempContext`
*   `StatusTempContext`
*   `InventoryTempContext`

Это позволяет типизировать ответ и избегать полей `None`.
