# Migration Log (История изменений)

[⬅️ Назад: Item System](../README.md)

---

## 🔄 Migration to Pydantic DTO (Jan 2026)

### Цель
Переход от `TypedDict` к строгим `Pydantic` моделям.

### Изменения
1.  **ResourceDTO, MaterialDTO, BaseItemDTO:** Созданы схемы.
2.  **ItemCoreData:** Базовый класс для инстансов.
3.  **WeaponData:**
    *   `damage_min/max` -> `power` (float).
    *   Добавлен `spread` (разброс).
    *   Добавлены `triggers`.
4.  **ArmorData:**
    *   `protection` -> `power` (float).
    *   Убраны лишние поля.

### Статус
✅ Схемы созданы.
⚠️ Требуется рефакторинг `ItemAssembler` под новые поля.
