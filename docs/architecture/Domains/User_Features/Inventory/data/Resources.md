# 📚 Inventory Resources & Data

[⬅️ Назад: Inventory Domain](../README.md)

---

## 🎯 Описание
Слой хранения статических данных, текстов и схем.
Здесь нет бизнес-логики, только данные и конфигурации.

**Root Path:** `backend/domains/user_features/inventory/data/`

---

## 📂 Структура Файлов

### 1. `inventory_resources.py`
**Role:** Хранилище текстов и фабрика кнопок.
*   **Texts:** Заголовки, описания, сообщения (константы).
*   **Buttons:** Методы для генерации списков `ButtonDTO` (фильтры, пагинация).

**Пример:**
```python
class InventoryResources:
    TITLE_MAIN = "👤 Экипировка"
    
    @staticmethod
    def get_filter_buttons(active: str) -> list[ButtonDTO]:
        return [...]
```

### 2. `inventory_constants.py`
**Role:** Константы домена.
*   `SECTION_NAMES`: Названия разделов.
*   `SLOT_MAPPING`: Маппинг слотов.
*   `PAGE_SIZE`: Размер страницы.

### 3. `schemas.py` (или в `dto/`)
**Role:** Pydantic модели для хранения данных в Redis.
*   `InventorySessionDTO`: Структура сессии.
*   `InventoryItemDTO`: Структура предмета.

---

## 🔄 Использование
`InventoryService` импортирует `InventoryResources` для наполнения `ViewDTO` текстами и кнопками.
