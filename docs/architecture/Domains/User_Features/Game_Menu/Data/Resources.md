# 📦 Resources & Locales

⬅️ [Back to Data Layer](./README.md)

> **Reference:** `backend/domains/user_features/game_menu/data/locales/menu_resources.py`

## 1. Purpose
Хранение статических данных для меню: тексты кнопок, иконки, шаблоны сообщений (если применимо).
Бэкенд отдает клиенту **данные** (DTO), но может также отдавать локализованные заголовки кнопок, чтобы клиент не хранил их у себя.

## 2. Structure Example

По аналогии с `onboarding_resources.py`, мы используем Python-классы или словари для хранения текстов.

```python
class MenuResources:
    # Button Labels
    BTN_INVENTORY = "📦 Inventory"
    BTN_STATUS = "ℹ️ Status"
    BTN_MAP = "🗺️ Map"
    
    # Layouts (Какие кнопки показывать в каких режимах)
    LAYOUT_EXPLORATION = ["inventory", "status", "map"]
    LAYOUT_COMBAT = ["status", "combat_log"] # В бою меню может быть урезано
```

## 3. Localization Strategy
В будущем, если потребуется мультиязычность, этот слой будет отвечать за выбор правильного текста в зависимости от `lang_code` пользователя.
Клиент получает уже готовый текст кнопки: `{"id": "inventory", "label": "📦 Инвентарь"}`.
