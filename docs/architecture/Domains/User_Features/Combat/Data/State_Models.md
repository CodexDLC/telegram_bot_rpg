# 💾 State Models (Session & Actor)

[⬅️ Назад: Data](../README.md)

---

## 🎯 Описание
Этот документ описывает **Состояние (State)** боевой системы.
Это данные, которые хранятся в Redis (персистентность) или в памяти Воркера (BattleContext) во время обработки хода.

**Источники:**
*   `apps/game_core/modules/combat/dto/combat_session_dto.py`
*   `apps/game_core/modules/combat/dto/combat_actor_dto.py`

---

## 1. 🌍 Session Layer (Контекст Боя)

### 1.1. `BattleContext` (In-Memory Root)
Корневой объект, который собирается `ContextLoader`'ом и передается в сервисы.
Содержит **все** данные, необходимые для обработки одного такта боя.

| Поле | Тип | Описание |
| :--- | :--- | :--- |
| `session_id` | `str` | UUID сессии. |
| `meta` | `BattleMeta` | Глобальные счетчики и состояние. |
| `actors` | `dict[str, ActorSnapshot]` | Словарь всех участников (ключ: `char_id` как строка). |
| `moves_cache` | `dict` | Кэш загруженных мувов (чтобы не ходить в Redis дважды). |
| `targets` | `dict[str, list[int]]` | Кэш целей (кто кого бьет). |
| `pending_logs` | `list[dict]` | Логи, накопленные за время обработки, но еще не сохраненные. |

### 1.2. `BattleMeta` (Redis: `:meta`)
Легковесные мета-данные сессии. Доступны для быстрого чтения (например, проверки статуса).

| Поле | Тип | Описание |
| :--- | :--- | :--- |
| `active` | `int` | Статус боя (1 - активен, 0 - завершен). |
| `step_counter` | `int` | Глобальный счетчик ходов/событий. |
| `active_actors_count` | `int` | Количество живых участников. |
| `teams` | `dict[str, list]` | Списки ID участников по командам (`red`, `blue`). |
| `winner` | `str \| None` | ID победившей команды. |
| `dead_actors` | `list` | Список ID погибших. |
| `battle_type` | `str` | Тип боя (PVE, PVP). |
| `location_id` | `str` | Где происходит бой. |

### 1.3. `CombatInitContextDTO`
DTO для инициализации новой сессии (передается в `CombatInitService`).

*   **`mode`**: Режим боя (`standard`, `duel`, `raid`).
*   **`teams`**: Список объектов `CombatTeamDTO`.

#### `CombatTeamDTO`
*   `players`: `list[int]` (ID игроков).
*   `pets`: `list[int]` (ID питомцев).
*   `monsters`: `list[str]` (ID шаблонов монстров, например `goblin_warrior`).

---

## 2. 👤 Actor Layer (Участник Боя)

Актор — это составная сущность. В коде он представлен классом `ActorSnapshot`, который собирает данные из нескольких ключей Redis.

### 2.1. `ActorSnapshot` (Aggregator)
Полный слепок состояния персонажа.

| Компонент | Тип | Описание |
| :--- | :--- | :--- |
| **`meta`** | `ActorMetaDTO` | "Горячие" данные (HP, Energy, Status). |
| **`raw`** | `ActorRawDTO` | "Холодные" данные (Атрибуты, Базовые статы). |
| **`loadout`** | `ActorLoadoutDTO` | Экипировка и доступные действия. |
| **`stats`** | `ActorStats` | **Рассчитанные** статы (In-Memory only). |
| `skills` | `dict` | Уровни скиллов. |
| `active_abilities` | `list[ActiveAbilityDTO]` | Динамические эффекты (баффы/дебаффы). |
| `xp_buffer` | `dict` | Накопленный опыт за бой. |

### 2.2. `ActorMetaDTO` (State)
Изменяемые данные, обновляются каждый ход.

*   **Identity:** `id`, `name`, `type` (player/monster), `team`, `is_ai`.
*   **Vitals:**
    *   `hp` / `max_hp`: Здоровье.
    *   `en` / `max_en`: Энергия.
*   **Status:**
    *   `is_dead`: Флаг смерти.
    *   `tactics`: Очки тактики (накапливаемый ресурс).
    *   `tokens`: Специальные токены (Combo points, Runes и т.д.).
*   **Feint System:**
    *   `hand`: `list[FeintCardDTO]` — Текущие доступные карты (финты).
    *   `pool_capacity`: `int` — Максимальный размер руки на следующий ход.
    *   `pool_used`: `int` — Сколько очков пула уже потрачено.

### 2.3. `ActorRawDTO` (Math Source)
Исходные данные для математического движка.

*   **`attributes`**: Базовые атрибуты (Strength, Agility, Intellect...). Структура: `{base: 10, source: {...}, temp: {...}}`.
*   **`modifiers`**: Постоянные модификаторы от пассивок/экипировки.

### 2.4. `ActorStats` (Calculated)
Результат работы `StatsEngine`. Эти данные **не хранятся** в базе, а вычисляются на лету из `raw` + `active_abilities`.

*   **`mods`** (`CombatModifiersDTO`): Финальные плоские значения (Physical Damage, Crit Chance, Evasion).
*   **`skills`** (`CombatSkillsDTO`): Значения скиллов.

### 2.5. `ActiveAbilityDTO` (Buffs/Debuffs)
Временные эффекты, висящие на персонаже.

*   `uid`: Уникальный ID инстанса эффекта.
*   `ability_id`: ID способности (например, `fire_dot`).
*   `source_id`: Кто наложил.
*   `expire_at_exchange`: Номер размена, когда эффект исчезнет.
*   `impact`: Влияние на Vitals (например, `{"hp": -10}` каждый ход).
*   `payload`: Доп. данные (стаки, сила замедления).
