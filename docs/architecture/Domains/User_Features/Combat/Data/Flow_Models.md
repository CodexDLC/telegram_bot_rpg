# 🌊 Flow Models (API, Action, Pipeline)

[⬅️ Назад: Data](../README.md)

---

## 🎯 Описание
Этот документ описывает **Поток Данных (Data Flow)**.
Это структуры, которые "путешествуют" между системами: от запроса клиента (API) через очередь задач (Worker) к ядру расчетов (Pipeline).

**Источники:**
*   `apps/common/schemas_dto/combat_source_dto.py` (External)
*   `apps/game_core/modules/combat/dto/combat_action_dto.py` (Action)
*   `apps/game_core/modules/combat/dto/combat_pipeline_dto.py` (Pipeline)

---

## 1. 🌐 External API (Client <-> Server)

### 1.1. Payloads (Входные данные)
Структуры, которые приходят в `handle_action` от Telegram-бота.

*   **`ItemPayload`**: Использование предмета.
    *   `item_id`: ID предмета.
    *   `target_id`: На кого использовать.
*   **`InstantPayload`**: Мгновенный скилл (без размена).
    *   `skill_id`: ID скилла.
    *   `target_id`: Цель.
*   **`ExchangePayload`**: Боевой размен (Атака).
    *   `target_id`: Кого бьем.
    *   `attack_zones`: Куда бьем (список зон).
    *   `block_zones`: Что защищаем.

### 1.2. `CombatDashboardDTO` (Выходные данные)
Снимок экрана боя для рендера сообщения в Telegram.

*   **`turn_number`**: Текущий ход.
*   **`status`**: Состояние боя.
*   **`hero`** (`ActorFullInfo`): Данные игрока (HP, Energy, Tokens, Effects).
*   **`target`** (`ActorFullInfo`): Данные текущей цели.
*   **`allies` / `enemies`** (`ActorShortInfo`): Списки для навигации.
*   **`logs`**: Список последних событий (`CombatLogEntryDTO`).

---

## 2. ⚡ Action Layer (Intent & Task)

### 2.1. `CombatMoveDTO` (Intent)
"Заявка" игрока на действие. Сохраняется в Redis (`moves:{char_id}`) до момента обработки.

| Поле | Описание |
| :--- | :--- |
| `move_id` | Уникальный ID заявки. |
| `char_id` | Кто совершает действие. |
| `strategy` | Тип стратегии (`item`, `instant`, `exchange`). |
| `payload` | Полиморфный контейнер с данными (см. Payloads). |
| `targets` | Список ID целей (резолвится перед обработкой). |

### 2.2. `CombatActionDTO` (Worker Task)
Задача, которая попадает в очередь `q:actions`. Содержит **все** необходимое для выполнения действия, чтобы воркер не зависел от внешнего состояния заявки.

*   **`action_type`**: Тип действия.
*   **`move`**: Основная заявка (Initiator).
*   **`partner_move`**: Встречная заявка (Target), если это размен (`exchange`).
*   **`is_forced`**: Флаг принудительного действия (например, пропуск хода по таймеру).

---

## 3. ⚙️ Pipeline Layer (Calculation Context)

Структуры, используемые внутри `CombatPipeline` для расчета результата взаимодействия.

### 3.1. `PipelineContextDTO` (The "Console")
Главный объект управления расчетом. Передается через все этапы (Check Accuracy -> Check Defense -> Calc Damage).

*   **`phases`**: Глобальные этапы (нужно ли считать пре-кальк, пост-кальк).
*   **`flags`**: Огромный набор флагов (см. ниже).
*   **`mods`**: Числовые модификаторы текущего удара (Accuracy Mult, Damage Boost).
*   **`triggers`**: Какие события сработали (Crit, Stun, Combo).
*   **`stages`**: Какие проверки выполнять (можно отключить уворот для гарантированного удара).

### 3.2. `PipelineFlagsDTO` (Switches)
Детализация флагов контекста.

*   **`ForceFlags`**: Абсолютные переключатели (`hit`, `crit`, `dodge` - гарантированно).
*   **`RestrictionFlags`**: Запреты (`cannot_crit`, `ignore_parry`).
*   **`DamageTypeFlags`**: Тип урона (`physical`, `fire`, `pure`...).
*   **`StateFlags`**: Внутреннее состояние комбо/серии ударов.

### 3.3. `InteractionResultDTO` (Result)
Итоговый отчет о взаимодействии. Возвращается из Pipeline.

*   **Facts:** `is_hit`, `is_crit`, `is_blocked`, `is_dodged`.
*   **Numbers:**
    *   `damage_raw`: Урон до брони.
    *   `damage_mitigated`: Сколько урона поглощено.
    *   `damage_final`: Итоговый урон в HP.
*   **Commands (`AbilityFlagsDTO`):** Инструкции для Ability Service (какие эффекты наложить).
    *   `apply_bleed`, `apply_stun`, `apply_slow`...
    *   `grant_counter_marker`...

---

## 4. 🔄 Async & Signals

### 4.1. `CollectorSignalDTO`
Сигнал для `CombatCollector` (сборщика заявок).
Отправляется в `arq:combat_collector`.

*   `signal_type`:
    *   `check_immediate`: Проверить мгновенное действие.
    *   `check_timeout`: Проверить таймер хода.
