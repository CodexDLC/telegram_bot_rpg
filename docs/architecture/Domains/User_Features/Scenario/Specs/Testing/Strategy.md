# 🧪 Scenario Testing Strategy

[⬅️ Назад: Scenario Domain](../../README.md)

---

## 🎯 Философия
Мы следуем глобальному [Testing Standard](../../../../../management/Standards/Testing_Standard.md).

### Специфика Scenario Domain
Сценарии — это графы переходов.
Главный фокус тестов — **навигация по графу** и **управление контекстом**.

---

## 📊 План Тестирования (Backend)

### 1. Unit Tests (Engine)
*   **Focus:** `ScenarioDirector` и `ScenarioEvaluator`.
*   **Принцип:** Чистая логика переходов без БД.

**Ключевые кейсы:**
*   `test_transition_condition_met`: Если условие (например, `has_item`) выполнено -> переход по ветке True.
*   `test_transition_condition_failed`: Если условие не выполнено -> переход по ветке False (fallback).
*   `test_action_execution`: При переходе выполняется действие (например, `give_reward`).
*   `test_formatter_substitution`: Подстановка переменных `{char_name}` в текст.
*   `test_button_filtering`: Скрытие кнопок, если условия их отображения не выполнены.

### 2. Integration Tests (Service)
*   **Focus:** `ScenarioService` + `Repository`.
*   **Принцип:** Проверка сохранения состояния сессии.

**Ключевые кейсы:**
*   `test_initialize_scenario`: Создание новой сессии, загрузка мастер-данных квеста.
*   `test_step_persistence`:
    1. Выполнить шаг.
    2. Проверить, что `current_node` обновилась в Redis.
    3. Проверить, что контекст (переменные) сохранился.
*   `test_finalize_transition`: Проверка, что при завершении квеста сервис возвращает правильный `CoreDomain` (например, `COMBAT`) и Payload.

### 3. Flow Tests (Quests)
*   **Focus:** Прохождение конкретных квестов (контент).

**Сценарии:**
*   **Tutorial Flow:** Пройти туториал от начала до конца, выбирая разные варианты ответов. Убедиться, что в конце выдаются награды.

---

## 📱 План Тестирования (Client)

### 1. Orchestrators (Unit)
*   **Focus:** Action Dispatching.
*   **Принцип:** Мокаем `ScenarioClient`.

**Кейсы:**
*   `test_handle_step`: При получении `step` экшена -> вызов API -> рендер новой сцены.
*   `test_domain_switch`: Если API вернул `current_state="combat"` -> вызов `Director.set_scene("combat")`.

### 2. UI Components (Unit)
*   **Focus:** Адаптивная клавиатура.

**Кейсы:**
*   `test_adaptive_keyboard`:
    *   Короткие кнопки -> 2 в ряд.
    *   Длинные кнопки -> 1 в ряд.
    *   Смешанные -> корректная группировка.
