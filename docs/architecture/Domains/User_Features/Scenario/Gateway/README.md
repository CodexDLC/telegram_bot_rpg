# 📂 Scenario Gateway Layer

[⬅️ Назад: Scenario Domain](../README.md)

---

## 🎯 Описание
Точка входа в бизнес-логику домена.
Изолирует API и другие домены от внутренней реализации сервисов.

## 🗺️ Содержание

### 📄 Классы
* **ScenarioGateway** (`backend/domains/user_features/scenario/gateway/scenario_gateway.py`)

---

## Методы

### Public (для API)

#### `initialize_scenario(...) -> CoreResponseDTO`
Запускает сценарий и возвращает DTO с заголовком.

#### `step_scenario(...) -> CoreResponseDTO`
Выполняет шаг. Если происходит переход в другой домен (например, Combat), возвращает `CoreResponseDTO` с новым `current_state`.

### Internal (для SystemDispatcher)

#### `get_entry_point(...) -> ScenarioPayloadDTO`
Возвращает чистый Payload (без обертки CoreResponseDTO) для использования внутри других процессов.
