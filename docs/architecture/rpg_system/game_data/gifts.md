# Gifts (Дары)

## 📋 Обзор

Дары (Gifts) — это магические или мутационные архетипы, которые определяют "класс" персонажа в плане магии.
Каждый Дар принадлежит определенной Школе (School) и предоставляет набор уникальных способностей (Abilities) и пассивных бонусов.

**Статус:** `gift_library.py` содержит прототипы. Требуется миграция в полноценную структуру `game_data/gifts/`.

## ⚙️ Техническая реализация

### Структура DTO

```python
from pydantic import BaseModel
from enum import Enum

class GiftSchool(str, Enum):
    FIRE = "fire"
    WATER = "water"
    AIR = "air"
    EARTH = "earth"
    LIGHT = "light"
    DARKNESS = "darkness"
    NATURE = "nature"
    ARCANE = "arcane"

class GiftDTO(BaseModel):
    gift_id: str                # "gift_true_fire"
    name_ru: str                # "Истинное Пламя"
    school: GiftSchool          # FIRE
    
    description: str
    role: str                   # "Damage Dealer", "Tank", etc.
    
    # Способности, открывающиеся по мере прокачки
    # Level -> List[AbilityID]
    abilities_progression: dict[int, list[str]] = {}
    
    # Пассивные бонусы (опционально)
    # Level -> Modifiers
    passives_progression: dict[int, dict] = {}
```

### Пример конфигурации (True Fire)

```python
true_fire_config = GiftDTO(
    gift_id="gift_true_fire",
    name_ru="Истинное Пламя",
    school=GiftSchool.FIRE,
    description="Твой огонь не требует топлива. Классический боевой пирокинез.",
    role="Damage Dealer",
    abilities_progression={
        1: ["fireball"],           # На 1 уровне
        3: ["flame_thrower"],      # На 3 уровне
        5: ["inferno_blast"]       # На 5 уровне
    }
)
```

## 📦 Структура файлов (Предлагаемая)

```
game_data/gifts/
├── __init__.py                 # Registry
├── schemas.py                  # GiftDTO
└── definitions/
    ├── fire.py                 # Все дары огня
    ├── water.py
    ├── ...
```

## 🔗 Связь с Abilities

Дар сам по себе не содержит логику заклинаний. Он содержит **список ID**, которые ссылаются на `ABILITY_REGISTRY`.
Это позволяет переиспользовать абилки (например, "Heal" может быть у разных даров воды) и держать конфиги чистыми.
