# Руководство для разработчика (Contributing Guide)

Добро пожаловать в команду разработки **Telegram Bot RPG**!
Этот документ описывает процессы разработки, стандарты кода и правила взаимодействия. Следование этим правилам обязательно для поддержания качества и стабильности проекта.

---

## 📋 Оглавление

1. [Подготовка окружения](#1-подготовка-окружения)
2. [Процесс разработки (Git Flow)](#2-процесс-разработки-git-flow)
3. [Стандарты кода](#3-стандарты-кода)
4. [Документация и AI](#4-документация-и-ai)
5. [Создание Pull Request (PR)](#5-создание-pull-request-pr)

---

## 1. Подготовка окружения

Мы используем Python 3.11+. Убедитесь, что у вас установлена эта версия.

### Шаг 1. Клонирование репозитория
```bash
git clone <ссылка_на_репозиторий>
cd telegram_bot_rpg
```

### Шаг 2. Создание виртуального окружения (venv)

Рекомендуется создавать venv в папке `.venv` (она уже добавлена в `.gitignore`).

**Windows:**
```bash
python -m venv .venv
.venv\Scripts\activate
```

**Linux / macOS:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### Шаг 3. Установка зависимостей

Мы разделяем зависимости на _боевые_ (для запуска бота) и _инструментальные_ (для разработки: линтеры, тесты).

```bash
pip install -r requirements-dev.txt
```

### Шаг 4. Настройка переменных окружения

1. Найдите файл `.env.example`.
2. Создайте его копию и назовите её `.env`.
3. Заполните `.env` вашими локальными токенами и доступами.

> ⚠️ **Важно:** Никогда не коммите файл `.env` в репозиторий!

---

## 2. Процесс разработки (Git Flow)

Мы используем упрощенную модель **Git Flow**.

### Основные ветки:
- `main` — **Production**. Стабильный код. Прямые коммиты запрещены.
- `develop` — **Pre-Production**. Основная ветка разработки.

### Правила работы:
1. **Всегда** обновляйте `develop` перед началом работы.
2. **Создавайте новую ветку** под каждую задачу (`feature/...`, `fix/...`, `refactor/...`).
3. **Коммиты:** Пишите осмысленные сообщения (`feat: ...`, `fix: ...`).

---

## 3. Стандарты кода

Мы строго следим за качеством кода. Конфигурация линтеров находится в `pyproject.toml`.

### Инструменты:
- **Ruff:** Линтинг и форматирование.
- **Mypy:** Строгая статическая типизация.

### Перед коммитом:
Вы **обязаны** запустить локальную проверку:

```bash
# 1. Проверка стиля и ошибок
ruff check .

# 2. Проверка типов
mypy .

# 3. Запуск тестов (если есть)
pytest
```

---

## 4. Документация и AI

### 📚 Где писать документацию?
Мы используем **Федеративную модель документации**.
*   Глобальные планы -> `docs/management/`.
*   Детали реализации -> `docs/architecture/Domains/{Domain}/Roadmap/`.

Подробнее см. в стандарте:
👉 **[Documentation Standard](./Standards/Documentation_Standard.md)**

### 🤖 Использование AI
Если вы используете AI для генерации кода или рефакторинга, следуйте гайдлайнам по Docstrings и комментариям.
AI должен не только писать код, но и объяснять его.

Подробнее см. в инструкции:
👉 **[AI Refactor Guidelines](../structure_manifest/AI_instructions/AI_REFACTOR_GUIDELINES.md)**

---

## 5. Создание Pull Request (PR)

Когда задача готова:
1. Запушьте вашу ветку (`git push origin feature/...`).
2. Создайте **Pull Request** в ветку `develop`.
3. Дождитесь прохождения CI (автотестов) и Code Review.
