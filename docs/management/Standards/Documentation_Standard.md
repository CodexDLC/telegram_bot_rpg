# 📜 Стандарт ведения документации и планирования

Этот документ описывает правила организации планов (Roadmaps), задач (Tasks) и технического долга (Tech Debt) в проекте. Мы используем **федеративную модель управления**: детали живут в доменах, глобальная стратегия — в центре.

---

## 1. Принципы разделения (Scope)

### 🌍 Global Scope (`docs/management/`)
Сюда попадают задачи и планы, которые:
1.  Касаются **всего проекта** (Инфраструктура, Релиз, CI/CD).
2.  Являются **кросс-доменными интеграциями** (затрагивают 2+ домена).
3.  Описывают **общую стратегию** (Фазы разработки).

### 🏰 Domain Scope (`docs/architecture/Domains/{Domain}/`)
Сюда попадают задачи и планы, которые:
1.  Касаются **только этого домена** (внутренняя логика, рефакторинг модуля).
2.  Детализируют глобальную задачу (например, "Реализация инвентаря" в глобальном плане -> список из 20 подзадач в доменном).

---

## 2. Структура файлов

### 📂 Global Level
```plaintext
docs/management/
├── Project_Roadmap.md       # Вехи (Phases) со ссылками на домены.
├── task/                    # Кросс-доменные и инфраструктурные задачи.
│   ├── Integration_Inv_Combat.md
│   └── Docker_Setup.md
└── tech_debt/               # Архитектурные и глобальные проблемы.
    └── Global_Refactoring.md
```

### 📂 Domain Level
Внутри каждого домена (например, `Combat`) создается папка `Roadmap/`:

```plaintext
docs/architecture/Domains/Combat/
├── Manifest.md              # Паспорт домена (что это и зачем).
├── ... (API, Engine, etc.)
└── Roadmap/                 # 📁 Управление доменом
    ├── README.md            # Основной план развития домена.
    ├── Tasks.md             # Список текущих мелких задач.
    └── Tech_Debt.md         # Локальный технический долг.
```

---

## 3. Правила работы с задачами

### 🔗 Cross-Domain Tasks (Интеграция)
Если задача требует изменений в `Inventory` и `Combat`:
1.  Создается задача в `docs/management/task/Integration_Inv_Combat.md`.
2.  В ней описывается суть интеграции.
3.  В доменных `Roadmap/README.md` (у Combat и Inventory) ставится ссылка на эту глобальную задачу.

### 🧹 Tech Debt (Технический долг)
*   **Локальный:** "Кривая функция расчета урона" -> `Combat/Roadmap/Tech_Debt.md`.
*   **Глобальный:** "Циклические импорты между модулями" -> `docs/management/tech_debt/Architecture_Debt.md`.

---

## 4. Формат Roadmap

### Global (`Project_Roadmap.md`)
```markdown
## Phase 3: Combat Update
* [ ] **Combat System:** Реализация ядра (см. [Combat Roadmap](../architecture/Domains/Combat/Roadmap/README.md))
* [ ] **Infrastructure:** Настройка Redis (см. [Task](../management/task/Redis_Setup.md))
```

### Domain (`Domains/Combat/Roadmap/README.md`)
```markdown
# Combat Roadmap

## Q1 Goals
* [ ] Реализовать расчет уклонения.
* [ ] Добавить 3 типа оружия.
```
