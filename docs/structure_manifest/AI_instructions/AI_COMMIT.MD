# 🤖 AI INSTRUCTION: GIT COMMIT MESSAGES

Этот документ описывает строгий стандарт того, как AI должен генерировать сообщения для коммитов в этом проекте.

---

## 1. АЛГОРИТМ РАБОТЫ
Когда пользователь просит "Сгенерируй коммит" или "Проверь статус и напиши коммит", AI должен:
1.  Выполнить команду `git status` (чтобы увидеть список измененных файлов).
2.  Выполнить команду `git diff` (чтобы понять суть изменений).
3.  Сгенерировать текст коммита по шаблону ниже.
4.  Вывести этот текст в чат в блоке кода (code block), чтобы пользователь мог его скопировать.

---

## 2. ФОРМАТ СООБЩЕНИЯ (TEMPLATE)

Сообщение должно состоять из двух частей: **Заголовка** и **Тела**.

### Часть 1: Заголовок (Header)
Первая строка. Максимум 50-70 символов.
Формат: `<TYPE>: <Краткое описание на русском или английском>`

**Допустимые типы (TYPES):**
*   `feat` — Новая функциональность (feature).
*   `fix` — Исправление ошибки (bug fix).
*   `docs` — Изменения только в документации.
*   `style` — Правки стилей (CSS), форматирования, отступов (не меняющие логику).
*   `refactor` — Правка кода без изменения функционала (оптимизация).
*   `chore` — Обслуживание, настройка сборки, конфиги, обновление зависимостей.

### Часть 2: Тело (Body)
Отделяется от заголовка одной пустой строкой.
Содержит маркированный список изменений, сгруппированный по смыслу или модулям.

---

## 3. ПРИМЕРЫ (EXAMPLES)

### Пример 1 (Документация и стили)
```text
docs: update design documentation and fix css paths

- **Documentation:**
  - Updated 01_identity.md to match Dark Luxury theme.
  - Updated 02_layout_navigation.md (Split Header).
  - Fixed Liquid syntax error in 05_content_map.md.

- **Styles & Assets:**
  - Fixed CSS paths in 404.html.
  - Replaced missing images with placeholders in team.html.
```

### Пример 2 (Новая фича)
```text
feat: add mobile menu logic

- **JS:**
  - Added toggleMenu() function for burger button.
  - Implemented click-outside listener.

- **CSS:**
  - Added .mobile-overlay styles.
  - Fixed z-index issues for header.
```

---

## 4. КОМАНДА ДЛЯ AI
Если пользователь пишет: *"Проверь статус и составь коммит"* — действуй строго по этой инструкции.